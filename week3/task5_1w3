def gcd(a, b):
    while b:
        a, b = b, a % b
    return a

A, B = map(int, input().split())
C, D = map(int, input().split())

num = A*D - C*B
den = B*D
g = gcd(abs(num), den)

print(num // g, den // g)
